<% content_for :body_content_left do %>
  <ul id="consults">
    <% @consults.each do |consult| %>
      <li>
        <%= link_to consult.user_name, refinery.fg_consult_path(consult) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
