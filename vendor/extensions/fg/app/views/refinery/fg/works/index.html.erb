<div class="container">
  <div class="row">
    <ul class="nav nav-pills pull-right">
      <li><%= link_to "全部作品", refinery.fg_works_path %></li>
    <% @work_categories.each do |c| %>
      <li><%= link_to c.name, refinery.fg_works_path(:category => c.name) %></li>
    <% end %>
    </ul>
  </div>
  <div class="BoardLayout" id="wrapper">
    <div id="ColumnContainer" style="height: 885px;">
      <div style="display: none;" class="feed">
      </div>
      <% @works.each do |work| %>
      <div data-height="400" data-width="500" data-id="<%= work.id %>" class="pin">
        <div class="PinHolder">
          <%= link_to(style_image_fu(work.main_image, "205x"), refinery.fg_work_path(work), :class => "PinImage ImgLink")%>
        </div>
        <p class="description"><%= work.name %></p>
      </div>      
      <% end %>
    </div><!-- #ColumnContainer -->

      <!-- Infinite scroll loading indicator -->
    <div id="LoadingPins">
      <span></span>
    </div>
    
  </div><!-- #wrapper -->
  <%= will_paginate @works, {:previous_label => '上一页', :next_label => '下一页'} %>
</div>

