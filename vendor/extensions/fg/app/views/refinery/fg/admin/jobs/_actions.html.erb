<ul>
  <% if ::Refinery::Fg::Admin::JobsController.searchable? %>
    <li>
      <%= render '/refinery/admin/search', :url => refinery.fg_admin_jobs_path %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), refinery.new_fg_admin_job_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? && ::Refinery::Fg::Admin::JobsController.sortable? && ::Refinery::Fg::Job.any? %>
  <li>
    <%= link_to t('.reorder', :what => "Jobs"),
                 refinery.fg_admin_jobs_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Jobs"),
                 refinery.fg_admin_jobs_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>
