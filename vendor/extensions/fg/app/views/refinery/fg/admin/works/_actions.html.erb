<ul>
  <% if ::Refinery::Fg::Admin::WorksController.searchable? %>
    <li>
      <%= render '/refinery/admin/search', :url => refinery.fg_admin_works_path %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), refinery.new_fg_admin_work_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? && ::Refinery::Fg::Admin::WorksController.sortable? && ::Refinery::Fg::Work.any? %>
  <li>
    <%= link_to t('.reorder', :what => "Works"),
                 refinery.fg_admin_works_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Works"),
                 refinery.fg_admin_works_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>
