<ul>
  <% if ::Refinery::Fg::Admin::ReportsController.searchable? %>
    <li>
      <%= render '/refinery/admin/search', :url => refinery.fg_admin_reports_path %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), refinery.new_fg_admin_report_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? && ::Refinery::Fg::Admin::ReportsController.sortable? && ::Refinery::Fg::Report.any? %>
  <li>
    <%= link_to t('.reorder', :what => "Reports"),
                 refinery.fg_admin_reports_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Reports"),
                 refinery.fg_admin_reports_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>
