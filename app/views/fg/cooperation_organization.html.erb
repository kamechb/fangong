<div class="container">
  <div class="row">
    <div class="span3" id="minor">
      <%= render_cell :sidebar, :online %>
      <%= render_cell :sidebar, :cooperation %>
    </div>    
    <div class="span9" id="main">
      <div class="well course">
        <div class="page-header">
          <h1>合作机构</h1>
        </div>
        <% @cooperations.each do |co| %>
          <%= image_fu co.logo, nil %>
        <% end %>
      </div>
    </div>
  </div>
</div>
